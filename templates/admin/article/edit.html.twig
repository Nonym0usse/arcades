{% extends '@EasyAdmin/default/edit.html.twig' %}

{% block main %}

    {% block entity_form %}
        {{ form_start(form) }}
            {{  form_row(form.name) }}
            {{  form_row(form.description, {type: 'fos_ckeditor'}) }}
            {{  form_row(form.image, {type: 'imageFile'}) }}
        {{ form_end(form) }}
    {%  endblock %}
{% endblock %}
